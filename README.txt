Sample application of reading in data from serial device and printing contents, such as data taken from a sensor, to a file

To run the program, do the following steps:
    - Specify the desired output file to print the data to
    - Specify the tty port you wish to read from
    - Run the command "make world"
    - Then run the program using "./run"